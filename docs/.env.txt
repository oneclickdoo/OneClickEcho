# Primer vrednosti za .env u korenu repoa (NE commitovati pravi .env sa tajnama).
# Docker Compose učitava .env za ${POSTGRES_PASSWORD} i env_file za api.

POSTGRES_PASSWORD=postavi-jaku-lozinku-ovde

# Postgres (servis "postgres" u compose-u) — Password mora biti ISTI kao POSTGRES_PASSWORD iznad.
ConnectionStrings__Database="Host=postgres;Port=5432;Database=oneclickecho;Username=oneclickecho_admin;Password=postavi-jaku-lozinku-ovde"

# Redis (ime servisa iz docker-compose.yml je "redis")
Redis__ConnectionString=redis:6379,abortConnect=false

# OpenIddict — generiši RSA i postavi issuer (vidi docs / prethodne poruke)
# OpenIddict (ASP.NET env names — loaded by api env_file; do not leave empty in production Docker)
OpenIddict__Issuer=https://viber.oneclick.rs
OpenIddict__RsaPrivateKeyPkcs8=generisi-openssl-base64-jedan-red

PUBLIC_UPLOADS_BASE_URL=https://viber.oneclick.rs/uploads
NEXT_PUBLIC_API_URL=https://viber.oneclick.rs/api
API_INTERNAL_URL=http://api:3901

# Opciono — Comtrade message id pod
# VIBER_CAMPAIGN_MESSAGE_ID_FLOOR=1000
